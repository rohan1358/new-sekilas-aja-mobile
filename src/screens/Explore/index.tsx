import { Base } from "@components";
import { primaryColor } from "@constants";
import React from "react";

const Explore = () => {
  return <Base barColor={primaryColor.main}></Base>;
};

export default Explore;
