import { adjust } from "../../utils";

export const spacing = {
  xxs: adjust(4),
  xs: adjust(8),
  s: adjust(12),
  sm: adjust(16),
  m: adjust(24),
  sl: adjust(32),
  l: adjust(40),
  xl: adjust(48),
  xxl: adjust(64)
};
