export const defaultValue = {
  regexEmail: /^([a-zA-Z0-9_\-\.]+)@([a-zA-Z0-9_\-\.]+)\.([a-zA-Z]{2,5})$/,
  textFieldState: {
    success: "success",
    none: "none",
    warn: "warn",
    fail: "fail",
  },
};
